@model List<TravelTripProject.Models.Entities.Blog>
@{ int i = 0;}
<div class="padding">
    <h4>En Popüler 10 Blogumuz</h4>

    @foreach (var item in Model)
    {
    <ul class="list list-pad">
        <li class="list-bg"><span>@{i++;} @i.</span><a href="/Blog/BlogDetail/@item.Id/">@item.Title</a></li>
    </ul>
    }

    <a href="#" class="link1"></a>
</div>
